const icm2ifs = 299792458.0 * 1e2 * 2.0 * π * 1e-15   # Reciprocal centimeter to inverse femtosecond
const icm2au = 1.0 / (1.0e-15 * 4.135667696e-6)       # Reciprocal centimeter to atomic unit
const icm2ev = 1.0 / (1.0e-15 * 1.602176634e-19)     # Reciprocal centimeter to electron volt
const ħ = (6.62607015e-34) / (2.0 * π)          # Dirac's constant
const kb = 1.380649e-23                          # Boltzmann's constant
